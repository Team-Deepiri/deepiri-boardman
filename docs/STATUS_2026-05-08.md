# Deepiri Boardman Status Snapshot (2026-05-08)

This is a repo-grounded snapshot of where Boardman stands as of 2026-05-08, compared to the 2026-05-03 vision discussion.

## Executive Snapshot

- Core GitHub -> Plaky sync is active and significantly expanded (issue mapping, PR linking, merge-state updates, QA transitions).
- Agent/chat and scan flows are implemented (CLI + API), with write guardrails and session memory.
- Deployment posture improved materially (runbook, preflight/smoke scripts, dockerized multi-service stack, worker split clarified).
- QA assignment and identity matching capabilities are now substantial (fuzzy + heuristic matching, optional LLM gray-zone resolver, worker endpoint).
- Biggest gap: strategic direction quality is not yet operationalized because `DIRECTION.md` is still template-level and there is no validated "high-quality scan output" baseline across repos.

## What Changed Since 2026-05-03

Recent merged PRs show active delivery in deployment, sync, and LLM/provider work:

- `#17` deployment runbook
- `#18` SQLite DB bootstrap for deploys
- `#19` deploy preflight checks
- `#20` deploy smoke checks
- `#13` sync_github_plaky improvements
- `#22` OpenRouter provider support
- `#24` dependency maintenance

## Vision Alignment (Boardman Vision)

### 1) Understand what each repository is trying to achieve

**Status: Partial**

- Implemented: `boardman init`, `boardman scan`, `boardman scan-all`, `boardman status`, repo registration/routing.
- Implemented: scan path uses repo context + LLM pipeline in `boardman/services/scan_handler.py` and CLI/API surfaces.
- Gap: top-level `DIRECTION.md` remains a template placeholder, so quality of "repo intent understanding" depends on per-repo docs that may not yet be initialized.

### 2) Detect missing or emerging work from repo signals

**Status: Partial-Strong**

- Implemented: PR/issue event handling in `boardman/services/pr_handler.py`, `boardman/services/issue_handler.py`.
- Implemented: fuzzy/heuristic PR-task linking and ambiguous PR triage path.
- Implemented: scan flow for missing work generation.
- Gap: no explicit quality benchmark yet proving consistently high-signal task generation across multiple repos.

### 3) Create and update Plaky tasks with clear ownership/routing

**Status: Strong**

- Implemented: create/update/subtask flows and richer mutations in `boardman/services/task_mutations.py`.
- Implemented: QA routing and assignment picker endpoints in `boardman/routes/assignment.py` and `boardman/assignment/qa_picker.py`.
- Implemented: board/group placement context and schema-aware patching.

### 4) Keep GitHub and Plaky context linked lifecycle-end-to-end

**Status: Strong**

- Implemented: issue->task mapping and PR link comments.
- Implemented: merge-state transitions with deferred completion when other PRs remain open.
- Implemented: review/review-comment transitions to QA states where configured/discoverable.

## 5/3 TODOs -> Current State

## Kyle (cloud/infrastructure/deploy, PAT/keys process)

**Status: Mostly done (infra/docs/scripts path)**

- Delivered deployment runbook: `docs/DEPLOYMENT.md`.
- Delivered preflight checks: `scripts/deploy_preflight.sh`.
- Delivered smoke checks: `scripts/deploy_smoke.sh`.
- Clarified architecture split between compose `boardman-worker` and Cloudflare worker (`worker/`).
- Key rotation procedure is documented in runbook.
- Remaining: production environment execution/compliance is process-dependent (outside repo proof).

## Poindexter (Plaky status columns to git, worker)

**Status: Substantially done**

- PR lifecycle status wiring exists for needs QA / in QA / merged handling in `boardman/services/pr_handler.py`.
- Internal worker-facing pick endpoint exists: `POST /api/v1/assignment/pick-qa` in `boardman/routes/assignment.py`.
- Queue/worker pattern present (`boardman-worker` in deploy docs + async enqueue flags in settings/routes).
- Remaining: full production verification of all board schemas/status-key variants.

## Quang (fuzzy/heuristics for QA + identity/title matching, deployed in worker)

**Status: Strongly done in code**

- Identity fuzzy matching with conservative scoring/ambiguity controls in `boardman/assignment/identity_match.py`.
- Repo/QA tier logic and classifier stack present (`boardman/assignment/*`, `scripts/sync_qa_capabilities.py`, `repo_signals.json`).
- Worker endpoint supports proxy mode and local fallback in `worker/src/index.ts`.
- OpenRouter integration landed (`boardman/settings.py`, `boardman/llm/*`).
- Remaining: stability depends on live data quality (GitHub org metadata + Plaky workspace consistency).

## Aditya (AI behavior/chat, Plaky API interaction, local scanning for direction->tasks)

**Status: Core capability implemented (ownership likely shared in commits)**

- Agent API + session history + delete session endpoints in `boardman/routes/agent.py`.
- Agent runtime with tool path, write guardrails (`allow_writes`), fallback chat, provider-aware errors in `boardman/agent/service.py`.
- CLI support: `boardman agent chat`, `boardman agent ask`, `boardman scan`, `boardman init`, `boardman scan-all`, `boardman status` in `boardman/cli/commands.py`.
- Plaky/GitHub tool integration in `boardman/agent/tools/*`.
- Remaining: behavior tuning + deterministic task quality + explicit human confirmation UX for broader board mutations in practice.

## Near-Term Outcomes Check

### Dependable webhook and CLI sync behavior

**Status: Good trend, likely near-ready**

- Strong implementation coverage plus deploy smoke/preflight scripts.
- Remaining: repeated live smoke runs on target infra and webhook replay testing cadence.

### Practical scan quality (meaningful, startable tasks)

**Status: In progress**

- Scan implementation is present.
- Needs controlled evaluation set, richer scan scope (considering modules, hot to-dos, and other contextual clues) and a quality rubric to confirm signal quality and non-duplication under real repos.

### Agent UX with read-first planning and explicit write confirmation

**Status: Partial**

- `allow_writes` is explicit and defaults to false.
- Tool policy and guardrail language is present.
- Remaining: broader confirmation checkpoints for risky/bulk mutations in real workflows and UI affordances.

## Current Risks / Gaps

- `DIRECTION.md` in this repo is still generic template content; this weakens "repo-grounded truth" for Boardman itself.
- Open PR/background task edge cases can still produce operational ambiguity without strong observability.
- QA tiering and identity heuristics are sophisticated, but can drift without periodic calibration against expected assignments.
- There is active local/uncommitted state in repo (`.env.example` modified), so release readiness should include a clean-branch checkpoint.

## Recommended Next Steps

1. Fill `DIRECTION.md` for Boardman itself with concrete current-phase goals and non-goals.
2. Define and run a scan quality eval on 3-5 target repos (precision of proposed tasks, dedupe rate, actionability score).
3. Add an operator runbook section for "safe write mode" defaults (`allow_writes=false` by default in production-facing flows).
4. Add lightweight metrics/log dashboards for webhook success, PR-link success, status patch failure reasons.
5. Run one full production-like smoke rehearsal (webhook ping, issue->task, PR open->comment/status, merge->status).

## Evidence Pointers

- Product/API/CLI overview: `README.md`
- Agent endpoints: `boardman/routes/agent.py`
- Agent runtime/guardrails: `boardman/agent/service.py`
- Sync lifecycle logic: `boardman/services/pr_handler.py`
- Assignment internals: `boardman/routes/assignment.py`, `boardman/assignment/qa_picker.py`
- Identity heuristics: `boardman/assignment/identity_match.py`
- Deployment runbook: `docs/DEPLOYMENT.md`
- Deploy checks: `scripts/deploy_preflight.sh`, `scripts/deploy_smoke.sh`
- Worker routing: `worker/src/index.ts`
