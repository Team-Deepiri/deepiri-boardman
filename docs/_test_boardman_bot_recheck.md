# Test PR — verify boardman[bot] auth after redeploy

Throwaway. Checking the QA-assignment comment posts as `deepiri-boardman[bot]` now that GITHUB_AUTH_MODE was re-set to github_app (see #133). Close without merging.
